# ── Core LLM & Orchestration ─────────────────────────────────────────────────
anthropic>=0.40.0              # Claude API — all agent LLM calls
openai>=1.50.0                 # Whisper transcription + text-embedding-3-small
langchain>=0.3.0               # Prompt templates, output parsers, LCEL chains
langchain-anthropic>=0.3.0     # LangChain ↔ Anthropic integration
langgraph>=0.2.0               # Multi-agent stateful graph orchestration
instructor>=1.4.0              # Structured output enforcement (Pydantic + Anthropic)

# ── API Framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0               # REST API server
uvicorn[standard]>=0.30.0      # ASGI server
python-multipart>=0.0.9        # File upload support for FastAPI

# ── Data & Validation ─────────────────────────────────────────────────────────
pydantic>=2.8.0                # Schema validation and typed models
pydantic-settings>=2.4.0       # Settings from .env file

# ── Memory & Storage ──────────────────────────────────────────────────────────
pinecone>=4.0.0                # Vector database for episodic memory (optional)
redis>=5.0.0                   # Short-term session state store (optional)

# ── Observability ─────────────────────────────────────────────────────────────
langsmith>=0.1.100             # LangGraph trace + token cost observability

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0           # Load .env file in development
httpx>=0.27.0                  # Async HTTP client (used internally by SDKs)
aiofiles>=23.0.0               # Async file operations

# ── Optional: Speaker Diarization (production upgrade) ───────────────────────
# pyannote.audio>=3.3.0        # Uncomment for real speaker diarization
# torch>=2.0.0                 # Required by pyannote
